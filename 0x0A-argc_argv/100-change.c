#include "holberton.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * main - change coins by money
 * @argc: length of arguments
 * @argv: arguments vector
 * Return: 1 or 0
 */
int main(int argc, char *argv[])
{
	if (argc - 1 != 1)
	{
		printf("%s\n");
		return (1);	
	}
	
	printf("%d\n", sum);
	return (0);
}
